version: "3.8"
services:
  fronttlc:
    container_name: fronttlc-container
    build: ./front
    ports: 
        - '4200:4200'
    command: > 
        bash -c "npm install && ng serve --host 0.0.0.0 --port 4200"
